const TreeNodeActions = () => {
  // return (
  //   <div
  //     className={classNames('justify-end', {
  //       flex: selected || !isVisible || hovered,
  //       hidden: !selected && isVisible && !hovered
  //     })}
  //   >
  //     <TreeNodeActionButton
  //       isVisible={!isVisible || selected || hovered}
  //       title={isVisible ? 'Hide' : 'Unhide'}
  //       onClick={handleClickVisibility}
  //       theme={theme}
  //     >
  //       {isVisible === true && <i className="fas fa-eye" />}
  //       {isVisible === false && <i className="fas fa-eye-slash" />}
  //     </TreeNodeActionButton>
  //     <TreeNodeActionButton isVisible={selected} title="Tools" onClick={handleClickTools} theme={theme}>
  //       <i className="fas fa-tools" />
  //     </TreeNodeActionButton>
  //     {canDelete && (
  //       <TreeNodeActionButton isVisible={selected} title="Remove" onClick={handleClickDelete} isRemoving>
  //         <i className="fas fa-trash-alt" />
  //       </TreeNodeActionButton>
  //     )}
  //   </div>
  // );
};

export default TreeNodeActions;
